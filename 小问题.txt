编程艺术12章问题：给两个css文件设置背景属性，一个设置background:red;另一个css文件设置background:url(xxx);图片不能正常显示。
解决：将第一个css文件里的背景颜色修改为background-color:red;
ajax跨域问题：
global.js:471 Access to XMLHttpRequest at from origin 'null' has been blocked by CORS policy: Cross origin requests are only supported for protocol schemes: http, data, chrome, chrome-extension, https.
